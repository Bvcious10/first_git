name: Test project
on: push
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Repo checkout
        uses: actions/checkout@v3
      - name: Install dependencies
        uses: npm ci
      - name: Run tests
        uses: npm test
